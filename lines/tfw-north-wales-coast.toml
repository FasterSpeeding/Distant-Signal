id = "tfw-north-wales-coast"
name = "North Wales Coast Line"
mode = "national-rail"
category = "main-line"
operators = [
  "AW",  # Transport for Wales -- principal TOC, hourly-ish stopping service.
  "VT",  # Avanti West Coast -- see sourcing note below on why this is
         # included (genuine scheduled stops, not just track-sharing).
]

destination_crs_filter = ["HHD", "CTR"]
# South, midpoint, near-north, terminus.
sample_stations = ["CTR", "RHL", "BNG", "HHD"]
match_keywords = ["North Wales Coast Line", "North Wales Main Line"]

# `destination_crs_filter` direction-coverage note (final review Important
# #3): originally `["HHD"]` only, which the sourcing note below already
# flags as a risk without having acted on it -- "the majority of Avanti
# Euston-Chester/North Wales trains terminate at Chester (1tph); only a
# minority (6/day) continue to Holyhead", so a `HHD`-only filter silently
# dropped the majority of Chester-terminating departures sampled at RHL/BNG.
# Since this file's own scope starts at Chester and doesn't extend south of
# it, `CTR` is added as a second anchor here -- the same extend-with-
# opposite-direction-anchor remedy `tfw-conwy-valley.toml` established
# (`["LLJ", "BFF"]`), rather than `tfw-marches.toml`'s omit-entirely remedy.
# This is safe: `destination_crs_filter` is only evaluated against
# departures sampled at this file's own `sample_stations` (`CTR`, `RHL`,
# `BNG`, `HHD`), and RHL/BNG/HHD are exclusive to this line -- no other
# catalogued line reaches them, so any departure there with destination
# `CTR` is unambiguously a North Wales Coast Line service. `CTR` is also
# shared with `tfw-marches.toml` at the station level, but a departure
# whose destination is also `CTR` is a logical impossibility at Chester
# itself (a train can't depart from and terminate at the same station), so
# adding `CTR` to the filter has no effect there and can't misclassify a
# Marches departure as North Wales Coast Line.

# Sourcing note (per the catalogue's "cross-checked against a second
# source" curation standard established for tfw-cambrian.toml,
# tfw-heart-of-wales.toml and tfw-conwy-valley.toml): all 7 CRS codes
# below that are new to this file (CTR, FLN, PRT, RHL, CWB, BNG, HHD)
# were confirmed from each station's own Wikipedia infobox "Station code"
# field, AND independently cross-checked against railwaycodes.org.uk's
# per-letter CRS/NLC/TIPLOC/STANME/STANOX pages (crsc/f/p/r/b/h.shtm),
# which returned an exact match for every one -- e.g. Chester really is
# CTR (not the more "obvious"-looking CHR), Prestatyn really is PRT (not
# PTA). Both sources were fetched live during this file's authoring; none
# of these codes are invented. TIPLOCs (CHST, FLINT, PSTA, RHYL, CBAY,
# BANGOR, HLYH) come from railwaycodes.org.uk's TIPLOC column directly;
# that field isn't shown on the Wikipedia infoboxes, so unlike the CRS
# codes it has only the one live source, consistent with TIPLOC's
# "optional, helps with TRUST/SCHEDULE correlation" status in
# lines/SCHEMA.md and the standard already used for TIPLOCs elsewhere in
# this catalogue. LLJ/LANDUDJ is not new -- it's carried over unchanged
# from `tfw-conwy-valley.toml`, itself already cross-checked there.
#
# Category: confirmed "main-line" (not "regional") via the line's own
# Wikipedia article, which describes it as "a major railway line" of
# 105.5 miles, predominantly double track, matching the treatment
# `west-coast-main-line.toml` gets for the same reason.
#
# Station-catalogue completeness fill-in (Task 4.5,
# `docs/superpowers/plans/2026-09-01-station-catalogue-completeness.md`):
# the 9 minor intermediate calls this file previously omitted -- Shotton
# (SHT), Conwy (CNW), Penmaenmawr (PMW), Llanfairfechan (LLF), and, on
# Anglesey after Bangor, Llanfairpwll (LPG), Bodorgan (BOR), Tŷ Croes
# (TYC), Rhosneigr (RHO) and Valley (VAL) -- are now listed below at
# their true geographic position. All 9 CRS codes were already confirmed
# from each station's own Wikipedia infobox by this file's original
# author (see the git history for this file's prior comment here); this
# task added the second source, directly re-fetching
# railwaycodes.org.uk's live per-letter CRS pages (crss/c/p/l/b/t/r/v.shtm)
# via raw HTML (a direct `curl` fetch, not the WebFetch tool that hit the
# truncation failure mode disclosed elsewhere in this batch), which
# returned an exact CRS match for all 9 and additionally supplied a
# TIPLOC for every one (SHOTTON, CONWY, PNMNMWR, LANFRFC, LFPW, BODORGN,
# TYCROES, RHOSNGR, VALLEY respectively) -- none guessed. Order and
# completeness of this list was independently re-confirmed against the
# line's own Wikipedia route diagram (Chester-Holyhead), which also
# re-confirmed that Queensferry, Connah's Quay, Bagillt, Mostyn,
# Talacre, Foryd, Old Colwyn, Llysfaen, Llandulas, Mochdre & Pabo, Aber,
# Menai Bridge, Britannia Bridge and Gaerwen are the *only* other closed
# stations on this route diagram (mostly 1966 Beeching cuts; Aber and
# Foryd similarly disused per their own Wikipedia infobox "Status:
# Disused" field). Abergele & Pensarn was originally, incorrectly,
# included in this same "closed" list -- it is real and currently open
# (see its own `[[stations]]` entry below, added by a later data-driven
# audit that caught the mistake against real scheduled trains) -- so the
# candidate set below is 10 stations, not 9.
#
# Llandudno itself (the town, CRS LLD) is deliberately out of scope: per
# the line's own route diagram, it sits on a short branch via Deganwy
# that leaves the through route at Llandudno Junction, distinct from
# both the Chester-Holyhead main line and the separate Conwy Valley
# Line branch (which diverges south at the same station). It isn't a
# through station on this line and isn't covered by the "minor
# intermediate calls" note above -- it's a different line's territory,
# not yet written up in this catalogue, so leaving it out here rather
# than guessing at a segment for it is the "minor intermediate calls
# omitted" pattern's sibling case, not an exception to it.
#
# Operator question (VT): confirmed Avanti West Coast genuinely
# timetables its own stopping service on this corridor rather than only
# transiting it. Avanti West Coast's own Wikipedia article states
# directly: "Six trains per day continue to Holyhead, calling at Flint,
# Prestatyn, Rhyl, Colwyn Bay, Llandudno Junction and Bangor" (part of
# the London Euston-Chester/Holyhead/Wrexham General service group).
# This is corroborated by the North Wales Coast Line's own, separately
# maintained Wikipedia article ("The principal through passenger
# services are London ... Avanti West Coast ...") and further by Avanti
# West Coast's own official website, whose global "Where we go"
# navigation lists both Chester and Bangor as served destinations (not
# just Holyhead as an endpoint reached by transiting). All three were
# fetched live. Per `west-coast-main-line.toml`'s own precedent of
# listing every TOC that genuinely timetables services on a shared
# line, VT is included here on that basis.
#
# Llandudno Junction segment-sharing decision (the call this file was
# asked to make -- see `tfw-conwy-valley.toml`'s comment, which
# deliberately left this open for this file to decide): station-overlap
# only, NOT a shared segment. LLJ below uses this file's own segment
# name (`tfw-north-wales-coast`), not `tfw-conwy-valley`. Reasoning:
# the two lines meet only at the LLJ station itself -- the Conwy Valley
# Line's own route diagram and Llandudno Junction's own Wikipedia
# article both show the Conwy Valley branch diverging south immediately
# at the station, with no stretch of track actually shared/run over by
# both lines' services beyond that single calling point. That's a
# genuine junction (interchange) but not a shared trunk of track, so an
# incident reported at Llandudno Junction should independently match
# both lines (each still sees the station in its own list) but as two
# separate `MatchScope::ExclusiveSegment` matches, not one propagated
# `MatchScope::SharedSegment` match -- see the regression test in
# `crates/aggregator/src/matcher.rs` for the concrete assertion.
#
# Cross-batch note, UPDATED (2026-09-21 real-world-sanity review): this
# file's own segment, `tfw-north-wales-coast`, below, is now genuinely
# SHARED with `lines/wcml-north-wales.toml` for the Chester-Holyhead
# stretch -- reversing the original station-overlap-only ruling described
# in the paragraph below (kept for history). Avanti's Euston-Holyhead
# service and this line's own local stopping service run over the exact
# same physical double-track main line for this entire stretch, not two
# corridors meeting at one station (unlike the genuinely distinct Chester/
# Wrexham corridor case -- see `tfw-shrewsbury-chester.toml`'s own Chester
# comment for that different situation). `wcml-north-wales.toml` reuses
# this file's own `tfw-north-wales-coast` segment name verbatim at its own
# 8 stations (CTR, FLN, PRT, RHL, CWB, LLJ, BNG, HHD -- exactly Avanti's
# own limited-stop calling pattern); this file's own additional 11
# stations on the same segment (Shotton, Abergele & Pensarn, Conwy,
# Penmaenmawr, Llanfairfechan and the five Anglesey stops) are on the same
# physical track too. `is_shared`/`is_exclusive_to` key off the segment
# *name* across the whole catalogue, not per-station, so an incident at
# one of those 11 is still classified `MatchScope::SharedSegment` for
# this file (correctly -- Avanti's own through service genuinely runs
# over that stretch too, even where it doesn't stop), even though
# `wcml-north-wales.toml` has no entry at any of those CRS codes and so
# cannot itself match such an incident at all. See
# `chester_station_overlap_matches_both_lines_as_exclusive`/
# `llj_station_overlap_matches_both_lines_as_exclusive` (both updated,
# where both files genuinely have a matching station) and
# `north_wales_coast_own_stretch_incident_is_shared_segment_but_single_match`/
# `tfw_north_wales_coast_new_station_incident_resolves_exclusive_segment`
# (the single-file-but-still-shared case) in `crates/common/src/matcher.rs`
# for the concrete assertions, and `lines/wcml-north-wales.toml`'s own
# "TfW/WCML shared corridor" note for the fuller writeup of this reversal.
#
# Original ruling, kept for history (controller ruling, reconciled during
# Task 11.8's batch verification): `lines/wcml-north-wales.toml` (Batch
# 1's Task 1.4) did not exist when this file was first written. It later
# landed with its own Chester-Holyhead segment (`wcml-north-wales-branch`)
# carrying an explicit "TfW future-overlap note" comment offering this
# file's curator exactly two options: reuse its segment name as a genuine
# shared trunk, or give this line its own distinct segment name with
# station-overlap-only at Chester. This file chose the second option,
# confirmed mutually consistent by Batch 11's own final review. That was a
# defensible reading of the evidence available at the time (the note only
# flagged the Chester station overlap specifically, not the full-corridor
# track sharing); the 2026-09-21 review re-examined the whole corridor and
# reversed it, per the updated note above.
[[stations]]
crs = "CTR"
tiploc = "CHST"
role = "junction"
segment = "tfw-north-wales-coast"
# Chester is a real multi-line junction (West Coast Main Line, the
# Wrexham/Borderlands corridor, the Wirral Line, the Mid-Cheshire Line
# and the Shrewsbury-Chester Line all meet here, per the line's own
# route diagram). Of those, `wcml-north-wales.toml` now genuinely shares
# this file's own segment name here (see the cross-batch note above); the
# others (`tfw-shrewsbury-chester.toml`, `merseyrail-wirral.toml`,
# `northern-mid-cheshire.toml`) remain station-overlap-only, each on its
# own distinct segment name.

[[stations]]
crs = "SHT"
tiploc = "SHOTTON"
role = "junction"
segment = "tfw-north-wales-coast"
# Shotton is also the junction for the Borderlands Line (Wrexham
# Central), per this station's own Wikipedia article -- no line in this
# catalogue reaches the Borderlands Line yet, so no shared-segment
# question arises here, and this file does not model or invent any
# Borderlands Line segment/stations.

[[stations]]
crs = "FLN"
tiploc = "FLINT"
role = "major"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "PRT"
tiploc = "PSTA"
role = "major"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "RHL"
tiploc = "RHYL"
role = "major"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "AGL"
tiploc = "ABGLELE"
role = "minor"
segment = "tfw-north-wales-coast"
# Abergele & Pensarn: this file's own sourcing note above (the "all
# closed/disused" list) incorrectly grouped this station with genuinely
# closed Beeching-era stops on the same route diagram. It is real and
# currently open -- confirmed via a data-driven audit against real
# scheduled trains (dozens of 2026-09-17 Liverpool-Llandudno workings
# show real booked arrival/departure times here) and independently via
# Wikipedia, which lists it as open on the North Wales Coast Line with
# Rhyl and Colwyn Bay as its immediate neighbours, exactly matching the
# schedule data. The other 14 names in that "closed" list were
# individually re-checked and are all correctly excluded (none have a
# live CRS code).

[[stations]]
crs = "CWB"
tiploc = "CBAY"
role = "major"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "LLJ"
tiploc = "LANDUDJ"
role = "junction"
segment = "tfw-north-wales-coast"
# Junction with the Conwy Valley Line (`tfw-conwy-valley.toml`) and,
# out of this file's scope, the short Llandudno branch via Deganwy. See
# the segment-sharing decision comment above the [[stations]] list.

[[stations]]
crs = "CNW"
tiploc = "CONWY"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "PMW"
tiploc = "PNMNMWR"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "LLF"
tiploc = "LANFRFC"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "BNG"
tiploc = "BANGOR"
role = "major"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "LPG"
tiploc = "LFPW"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "BOR"
tiploc = "BODORGN"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "TYC"
tiploc = "TYCROES"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "RHO"
tiploc = "RHOSNGR"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "VAL"
tiploc = "VALLEY"
role = "minor"
segment = "tfw-north-wales-coast"

[[stations]]
crs = "HHD"
tiploc = "HLYH"
role = "terminus"
segment = "tfw-north-wales-coast"
# Terminus, and the ferry interchange for Ireland (Dublin Port).
