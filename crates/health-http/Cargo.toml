[package]
name = "health-http"
version = "0.1.0"
edition = "2024"

[dependencies]
axum = { version = "0.8.9", features = ["http2"] }
common = { path = "../common" }
metrics = "0.24"
tokio = { version = "1.53.1", features = ["rt-multi-thread", "macros", "net"] }
tracing = "0.1.44"
